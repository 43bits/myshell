🐚 MyShell — Custom Unix Shell (OS Project)

A fully functional Unix-like shell built in C with support for pipes, redirection, background jobs, and built-in commands.

Custom Shell with Piping & Job Control
Unix shell from scratch in C — with fork(), exec(), pipes (|), background jobs (&), and signal handling (Ctrl+C)